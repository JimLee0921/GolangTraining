# slices 包

Go 1.21 +引入，主要用了泛型作为基础，对 slice 的常用操作进行封装

在 slices 包引入前，对于 slice 可能需要进行某些操作但是没有官方 API 支持：

- 判断是否包含某个元素
- 去重 / 删除 / 插入
- 排序+二分查找
- 比较两个 slice 是否相等
- 就地修改 vs 返回新的 slice 语义混乱

## 目标

slices 包主要为任意元素类型的 slice 提供一组类型安全、可读、性能可控的通用算法，
主要提供的不是数据结构，而是对 `[]T` 的通用操作算法。


> 这个包跟 cmp 和 iter 包有较大关联