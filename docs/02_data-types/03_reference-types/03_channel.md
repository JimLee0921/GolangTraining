# 通道

## 核心概念
- 通道负责 Goroutine 之间的通信，类型为 `chan T`。
- 可选择无缓冲或有缓冲以控制同步行为。

## 语法要点
- 使用 `make(chan T, size)` 创建，`size` 为 0 时表示无缓冲。
- `<-` 操作符用于发送与接收，`close` 结束发送。

## 实用示例
- 管道模式、工作池、信号通知。
- 配合 `select` 实现超时与多路复用。

## 常见陷阱
- 禁止在接收端关闭通道，避免 panic。
- 防止 Goroutine 泄漏：确保所有发送/接收路径完成。
