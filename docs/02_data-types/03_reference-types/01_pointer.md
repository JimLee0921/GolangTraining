# 指针

## 核心概念
- 指针保存变量的内存地址，可用于共享或修改底层值。
- `nil` 表示空指针，使用前需判空。

## 语法要点
- 使用 `&` 取地址，`*` 解引用。
- `new(T)` 返回 `*T`，`make` 不适用于指针。

## 实用示例
- 函数参数修改调用方数据。
- 构建链表、树等引用数据结构。

## 常见陷阱
- 避免悬垂指针，确保引用对象生命周期有效。
- 注意 `nil` 接收者方法的特殊处理。
