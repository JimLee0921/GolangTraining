# 接口

## 核心概念
- 接口定义行为集合，实现由方法集匹配隐式完成。
- 空接口 `interface{}` 可容纳任意类型。

## 语法要点
- 类型断言 `value.(T)` 与类型选择 `switch value.(type)`。
- 接口值由动态类型与动态值组成。

## 实用示例
- 依赖倒置、面向接口编程。
- 标准库中的 `io.Reader`、`error` 等常用接口。

## 常见陷阱
- 使用 `nil` 接口时，动态类型与动态值都为 `nil` 才等于 `nil`。
- 小心接口内嵌拷贝的零值与方法集差异。
