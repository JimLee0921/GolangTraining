# 函数

## 核心概念
- 函数是头等公民，可存储于变量、作为参数和返回值。
- 支持闭包，捕获外部变量。

## 语法要点
- 声明形式 `func (接收者) 名称(参数) (返回值)`。
- 支持可变参数、命名返回值、匿名函数。

## 实用示例
- 回调、装饰器模式、并发任务封装。
- 与 `defer` 结合实现资源释放。

## 常见陷阱
- 闭包捕获迭代变量需注意作用域。
- 递归调用需有明确终止条件与栈空间评估。
