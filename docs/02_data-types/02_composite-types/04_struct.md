# 结构体

## 核心概念
- 结构体由字段组合而成，描述一组相关数据。
- 支持匿名字段与嵌入，实现组合复用。

## 语法要点
- 字面量初始化支持命名字段与按顺序赋值。
- 标签 `tag` 用于序列化、验证等元信息。

## 实用示例
- 定义领域模型、配置项、API 请求响应。
- 结合方法集，实现面向接口编程。

## 常见陷阱
- 值接收者与指针接收者的选择影响方法集。
- 嵌入字段同名冲突需要显式限定。
