# 映射

## 核心概念
- 映射(Map)以哈希表实现，用键值对存储动态数据。
- 键必须是可比较类型。

## 语法要点
- 使用 `make(map[K]V)` 或字面量创建。
- 访问键时返回值和布尔存在标记 `(value, ok)`。

## 实用示例
- 统计、分组和快速查找。
- 嵌套映射实现二维索引。

## 常见陷阱
- 并发写需借助互斥锁或 `sync.Map`。
- 迭代顺序不保证稳定，必要时对键排序。
