package main

func main() {
	/*
		在 Go 并发编程中，Pipeline（流水线）模式 是一种常见的设计思路：
			把一个大的任务拆解成多个阶段（stage）
			每个阶段由一个或多个 goroutine 处理，并通过 channel 连接
			数据像水流一样，从上游流向下游，每个阶段做自己的事
		就像现实里的工厂流水线：
			第一道工序负责把零件放到传送带上
			第二道工序负责打磨
			第三道工序负责上漆
			最终产出成品
		1. 每个 stage 都有输入和输出 channel
			输入：接收上游的数据
			输出：把处理结果交给下游
		2. goroutine 独立执行
			每个 stage 都在 goroutine 中运行，互不阻塞
		3. 数据流向单向
			只要上游继续发送数据，下游就能不断消费
		4. 关闭 channel 表示结束
			当上游关闭 channel，下游的 range 会自然结束
	*/
}
